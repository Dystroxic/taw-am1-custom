#define COMPONENT viewDistance
#define COMPONENT_BEAUTIFIED View Distance
#include "..\main\script_mod.hpp"

// Disable function caching so it rebuilds the function each time (to ensure always most recent)
#define DISABLE_COMPILE_CACHE

#include "..\main\script_macros.hpp"

// Include defines for this specific component
#include "defines.hpp"